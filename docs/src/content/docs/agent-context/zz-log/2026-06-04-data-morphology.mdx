---
title: "Data morphology — the forms structured data takes, and the operations between them"
description: Names and sketches "data morphology" — Crosswalker's first-principles taxonomy of the forms structured data takes (data shape + view shape) and the operations that transform between them. Grounded in relational algebra, OLAP aggregation, and graph traversal; separates the kinds of operation (combine/join vs summarize/aggregate vs traverse) that collapse into "the matrix"; maps onto query-primitives (Layer A) + view-shapes (Layer B); proposes the data-shape layer and a candidate-view menu for the GRC corpus.
sidebar:
  order: -20260604
  label: "06-04 · Data morphology (idea capture)"
tags: [log, taxonomy, query-engine, ontology, relational-algebra, views]
date: 2026-06-04
---

import { Aside } from '@astrojs/starlight/components';

<Aside type="note" title="What this is — and why over-scoping is fine here">
A morning-read idea dump. The intent (user, 2026-06-04): build a first-principles taxonomy *inside Crosswalker* of how data is represented and related — because the project is, at heart, a tool for understanding **the shape of ontologies** and how to use them. Joining, left-joining, anti-joining — but also things that aren't joins at all, like summing. "I don't care if it's over-scoping" — this *is* the learning core of the project. The payoff: knowing the taxonomy tells us **which views to build** as we test through the GRC audit/compliance library.
</Aside>

## The name: data morphology

**Data morphology** — the study of the *forms* structured data takes, and the operations that transform one form into another. ("Morphology" is the study of form.) This is **not a general taxonomy**; it is the specific thing Crosswalker must describe to reason about ontologies well: every concept set has a *shape*, every view is a *shape*, and every operation is a *transformation between shapes*. Having the name lets us say precisely — *what shape is this, which operation am I applying, what shape am I rendering* — instead of blurring all three into "the table."

## The mental model: shape, then three knobs

The goal is an **intuitive mental model**, not an academic chart. Hold this: data has a **shape**, and then there are **three independent knobs**. Almost every confusion (*"is a pivot a join?"*, *"transpose vs x/y axes"*) is from mixing the knobs.

> **Shape** what you've got → turn three knobs: **what it says** · **how it's laid out** · **how it looks**.

| | Plain question | The knob controls… | Everyday words |
|---|---|---|---|
| **Shape** | what *kind* of thing is this? | the native form | list · tree · web · timeline |
| **① Transform** | what should it **say**? | the *content* — which facts are present | keep (`filter`) · merge (`join`) · total (`sum`) |
| **② Reshape** | how is it **laid out**? | the *grid layout* — same facts, moved | `transpose` · wide ↔ tall · roll-up / drill-down |
| **③ Draw** | how should it **look**? | the *picture* — facts → visuals | x / y axes · color · bars vs dots |

**The one rule that resolves everything:** the three knobs are independent.
- **Transform** changes the *facts* — a sum removes rows, a join adds columns.
- **Reshape** changes the *layout*, never the facts — transpose is the same data, spun.
- **Draw** changes the *picture*, never the layout — x/y axes is a visual choice.

<div class="not-content" style="margin:1.6rem 0; padding:1.1rem 1.2rem; border:1px solid var(--sl-color-hairline); border-radius:14px; background:var(--sl-color-bg-nav);">
<div style="text-align:center; font-size:.82em; color:var(--sl-color-gray-2); margin-bottom:.7rem;">One little spreadsheet — <strong>coverage by CSF function</strong></div>
<div style="display:flex; justify-content:center;"><table style="border-collapse:collapse; font-size:.85em; border:1px solid var(--sl-color-hairline); border-radius:8px; overflow:hidden;"><thead><tr><th style="padding:.34rem 1.2rem; background:var(--sl-color-bg); color:var(--sl-color-text-accent); border-bottom:2px solid var(--sl-color-accent); font-weight:700;">function</th><th style="padding:.34rem 1.2rem; background:var(--sl-color-bg); color:var(--sl-color-text-accent); border-bottom:2px solid var(--sl-color-accent); font-weight:700; text-align:right;">controls</th></tr></thead><tbody><tr><td style="padding:.3rem 1.2rem;">GV</td><td style="padding:.3rem 1.2rem; text-align:right;">20</td></tr><tr><td style="padding:.3rem 1.2rem; border-top:1px solid var(--sl-color-hairline);">DE</td><td style="padding:.3rem 1.2rem; border-top:1px solid var(--sl-color-hairline); text-align:right;">9</td></tr><tr><td style="padding:.3rem 1.2rem; border-top:1px solid var(--sl-color-hairline);">PR</td><td style="padding:.3rem 1.2rem; border-top:1px solid var(--sl-color-hairline); text-align:right;">15</td></tr></tbody></table></div>
<div style="text-align:center; margin:.95rem 0 .8rem; font-size:.66em; letter-spacing:.14em; color:var(--sl-color-gray-3);">▼ THREE INDEPENDENT KNOBS ▼</div>
<div style="display:flex; flex-wrap:wrap; gap:.7rem; align-items:stretch;">
<div style="flex:1 1 175px; display:flex; flex-direction:column; border:1px solid var(--sl-color-hairline); border-left:3px solid #f5a623; border-radius:10px; padding:.7rem .85rem; background:rgba(245,166,35,.08);"><div style="font-size:.74em; font-weight:700; color:#f5a623;">① TRANSFORM</div><div style="font-size:.72em; color:var(--sl-color-gray-2); margin-top:.12rem;">the facts change · total → fewer rows</div><div style="flex:1; display:flex; align-items:center; justify-content:center; min-height:74px;"><table style="border-collapse:collapse; font-size:.82em;"><tbody><tr><td style="border:1px solid var(--sl-color-hairline); padding:.3rem .65rem; color:var(--sl-color-gray-2);">TOTAL</td><td style="border:1px solid var(--sl-color-hairline); padding:.3rem .65rem; text-align:right; font-weight:700; font-size:1.25em;">44</td></tr></tbody></table></div></div>
<div style="flex:1 1 175px; display:flex; flex-direction:column; border:1px solid var(--sl-color-hairline); border-left:3px solid #4aa3df; border-radius:10px; padding:.7rem .85rem; background:rgba(74,163,223,.08);"><div style="font-size:.74em; font-weight:700; color:#4aa3df;">② RESHAPE</div><div style="font-size:.72em; color:var(--sl-color-gray-2); margin-top:.12rem;">same facts, spun · transpose</div><div style="flex:1; display:flex; align-items:center; justify-content:center; min-height:74px;"><table style="border-collapse:collapse; font-size:.82em;"><tbody><tr><td style="border:1px solid var(--sl-color-hairline); padding:.24rem .58rem; font-weight:600;">GV</td><td style="border:1px solid var(--sl-color-hairline); padding:.24rem .58rem; font-weight:600;">DE</td><td style="border:1px solid var(--sl-color-hairline); padding:.24rem .58rem; font-weight:600;">PR</td></tr><tr><td style="border:1px solid var(--sl-color-hairline); padding:.24rem .58rem; text-align:right;">20</td><td style="border:1px solid var(--sl-color-hairline); padding:.24rem .58rem; text-align:right;">9</td><td style="border:1px solid var(--sl-color-hairline); padding:.24rem .58rem; text-align:right;">15</td></tr></tbody></table></div></div>
<div style="flex:1 1 175px; display:flex; flex-direction:column; border:1px solid var(--sl-color-hairline); border-left:3px solid #3fb950; border-radius:10px; padding:.7rem .85rem; background:rgba(63,185,80,.08);"><div style="font-size:.74em; font-weight:700; color:#3fb950;">③ DRAW</div><div style="font-size:.72em; color:var(--sl-color-gray-2); margin-top:.12rem;">becomes a picture · x = fn, height = count</div><div style="flex:1; display:flex; align-items:flex-end; justify-content:center; gap:14px; min-height:74px; padding-bottom:.15rem;"><div style="display:flex; flex-direction:column; align-items:center;"><div style="width:22px; height:50px; background:#3fb950; border-radius:3px 3px 0 0;"></div><div style="font-size:.62em; color:var(--sl-color-gray-2); margin-top:3px;">GV·20</div></div><div style="display:flex; flex-direction:column; align-items:center;"><div style="width:22px; height:23px; background:#3fb950; border-radius:3px 3px 0 0;"></div><div style="font-size:.62em; color:var(--sl-color-gray-2); margin-top:3px;">DE·9</div></div><div style="display:flex; flex-direction:column; align-items:center;"><div style="width:22px; height:38px; background:#3fb950; border-radius:3px 3px 0 0;"></div><div style="font-size:.62em; color:var(--sl-color-gray-2); margin-top:3px;">PR·15</div></div></div></div>
</div>
<div style="text-align:center; font-size:.8em; color:var(--sl-color-gray-2); margin-top:.85rem;">Same table, three knobs → three results. <strong>They don't interfere.</strong></div>
</div>

"Is a pivot a join?" No — a pivot turns all three knobs at once (total → spread wide → put on axes), which is why it feels like one big thing. Pull the knobs apart and it's obvious. Everything below is the rigorous backing for these four moves.

## The instinct that started this: a pivot is not a join

"A pivot could also be a left join — is that a thing?" No — and the *no* is the whole point. **Joining combines rows; summing collapses them. They are different families of operation.** A pivot is fundamentally a **group-and-aggregate** (OLAP), not a join. The join is an *optional upstream step* (to pull in unmapped rows so gaps show). Disentangling that is what this taxonomy is for. (See the [four dials log](/crosswalker/agent-context/zz-log/2026-06-03-four-dials-join-shape-layout-view/) — this extends it from 4 dials into a fuller stack.)

## The full stack (the meta-taxonomy)

Five orthogonal layers. Each is an independent choice; reading top-to-bottom is the lifecycle of a question.

| Layer | The question | Choices | Where it lives today |
|---|---|---|---|
| **1. Data shape** | how is the data *represented*? | relation/table · tree/hierarchy · **graph** · sequence | implicit — *no concept page yet* |
| **2. Relations** | how do items *relate*? | hierarchical (broader/narrower) · associative (crosswalk) · membership · evidence · temporal | [terminology](/crosswalker/concepts/terminology/), partly |
| **3. Operations** | what do you *do* to it? | reshape · combine · summarize · traverse · compare | [query-primitives](/crosswalker/concepts/query-primitives/) (flat list of 8) |
| **4. View shape** | how is the result *drawn*? | table · list · pivot · graph · hierarchy · timeline | [view-shapes](/crosswalker/concepts/view-shapes/) ✅ |
| **5. Layout + settings** | where do notes *live*; knobs | folder/file/heading/tag/wikilink; rowsBy/heatmap/sort | [hierarchy-primitives](/crosswalker/concepts/hierarchy-primitives/) ✅ |

## Operation families — the meat (and the join-vs-sum split)

The existing [query-primitives](/crosswalker/concepts/query-primitives/) page lists 8 operations *flat*. The missing first-principles move is to **group them by what they fundamentally do to a row-set**, grounded in established theory:

| Family | What it does to rows | Primitives (ours) | Theory it comes from |
|---|---|---|---|
| **Reshape / select** | keep/drop rows + columns; derive new columns | `filter` · `project` · `bind` | relational algebra (σ select, π project) |
| **Combine / relate** | *widen* — stitch two row-sets on a key | `join` (inner/left/right/full/**anti**/semi) · `set-op` (∪ ∩ −) | relational algebra (⋈, ∪, −) |
| **Summarize / reduce** | *collapse* — many rows → one summary per group | `aggregate` (group-by + count/sum/avg/min/max/density) | **OLAP** — a GROUP BY *extension* to classic RA, not RA itself |
| **Traverse** | *follow edges* — walk the graph, transitively | `traverse` (hop · transitive closure · neighborhood) | Datalog / SPARQL property paths (`p*`) — beyond RA |
| **Compare** | delta between two snapshots | `diff` | version/ontology-specific |

**The key distinctions to internalize:**
- **Combine widens, summarize collapses.** A join adds columns (more about each row); an aggregate removes rows (fewer, summarized rows). Opposite directions.
- **A pivot = combine (assemble) → summarize (group by row-axis × col-axis, aggregate a measure) → present as a matrix.** So a pivot belongs to the *summarize* family wearing a *view shape*. "Pivot = left join" was a category error: the left join, if present, is the *combine* step feeding it.
- **Traverse is its own thing.** Following `broader`/`maps-to` edges transitively (CSF → 800-53 → CRI) is neither a join nor an aggregate — it's graph reachability (recursive). Classic RA can't even express it; that's why Tier 2 uses recursive CTEs.

## Data shapes — the layer with no page yet

The deepest first-principles point, and the one we haven't written down:

<div class="not-content" style="display:flex; flex-wrap:wrap; gap:.6rem; margin:1.2rem 0;">
<div style="flex:1 1 130px; display:flex; flex-direction:column; align-items:center; border:1px solid var(--sl-color-hairline); border-radius:10px; padding:.85rem .6rem .8rem; background:var(--sl-color-bg-nav);"><svg viewBox="0 0 64 44" style={{width:'58px',height:'40px',marginBottom:'.45rem'}}><rect x="13" y="8" width="38" height="7" rx="2" fill="var(--sl-color-text-accent)"/><rect x="13" y="19" width="38" height="7" rx="2" fill="var(--sl-color-text-accent)" opacity="0.7"/><rect x="13" y="30" width="38" height="7" rx="2" fill="var(--sl-color-text-accent)" opacity="0.45"/></svg><div style="font-weight:600; font-size:.85em;">List / table</div><div style="font-size:.72em; color:var(--sl-color-gray-2);">rows of facts</div></div>
<div style="flex:1 1 130px; display:flex; flex-direction:column; align-items:center; border:1px solid var(--sl-color-hairline); border-radius:10px; padding:.85rem .6rem .8rem; background:var(--sl-color-bg-nav);"><svg viewBox="0 0 64 44" style={{width:'58px',height:'40px',marginBottom:'.45rem'}}><line x1="32" y1="12" x2="19" y2="31" stroke="var(--sl-color-gray-3)" strokeWidth="2"/><line x1="32" y1="12" x2="45" y2="31" stroke="var(--sl-color-gray-3)" strokeWidth="2"/><circle cx="32" cy="10" r="5.5" fill="var(--sl-color-text-accent)"/><circle cx="19" cy="34" r="5.5" fill="var(--sl-color-text-accent)"/><circle cx="45" cy="34" r="5.5" fill="var(--sl-color-text-accent)"/></svg><div style="font-weight:600; font-size:.85em;">Tree</div><div style="font-size:.72em; color:var(--sl-color-gray-2);">nested, one parent</div></div>
<div style="flex:1 1 130px; display:flex; flex-direction:column; align-items:center; border:1px solid var(--sl-color-accent); border-radius:10px; padding:.85rem .6rem .8rem; background:var(--sl-color-accent-low);"><svg viewBox="0 0 64 44" style={{width:'58px',height:'40px',marginBottom:'.45rem'}}><line x1="16" y1="11" x2="48" y2="13" stroke="var(--sl-color-gray-3)" strokeWidth="1.5"/><line x1="16" y1="11" x2="14" y2="35" stroke="var(--sl-color-gray-3)" strokeWidth="1.5"/><line x1="16" y1="11" x2="46" y2="34" stroke="var(--sl-color-gray-3)" strokeWidth="1.5"/><line x1="48" y1="13" x2="46" y2="34" stroke="var(--sl-color-gray-3)" strokeWidth="1.5"/><line x1="14" y1="35" x2="46" y2="34" stroke="var(--sl-color-gray-3)" strokeWidth="1.5"/><circle cx="16" cy="11" r="5" fill="var(--sl-color-text-accent)"/><circle cx="48" cy="13" r="5" fill="var(--sl-color-text-accent)"/><circle cx="14" cy="35" r="5" fill="var(--sl-color-text-accent)"/><circle cx="46" cy="34" r="5" fill="var(--sl-color-text-accent)"/></svg><div style="font-weight:600; font-size:.85em;">Graph / web</div><div style="font-size:.72em; color:var(--sl-color-gray-2);">many-to-many · <em>the substrate</em></div></div>
<div style="flex:1 1 130px; display:flex; flex-direction:column; align-items:center; border:1px solid var(--sl-color-hairline); border-radius:10px; padding:.85rem .6rem .8rem; background:var(--sl-color-bg-nav);"><svg viewBox="0 0 64 44" style={{width:'58px',height:'40px',marginBottom:'.45rem'}}><line x1="8" y1="22" x2="56" y2="22" stroke="var(--sl-color-gray-3)" strokeWidth="2"/><circle cx="16" cy="22" r="4" fill="var(--sl-color-text-accent)"/><circle cx="32" cy="22" r="4" fill="var(--sl-color-text-accent)"/><circle cx="48" cy="22" r="4" fill="var(--sl-color-text-accent)"/></svg><div style="font-weight:600; font-size:.85em;">Timeline</div><div style="font-size:.72em; color:var(--sl-color-gray-2);">ordered, in time</div></div>
</div>

| Shape | Structure | In Crosswalker |
|---|---|---|
| **Relation / table** | a set of tuples (rows × columns) | a *projection* of concepts/edges for one question |
| **Tree / hierarchy** | mono-parent nesting | a framework's internal structure (SKOS `broader`) |
| **Graph** | nodes + typed edges, many-to-many | **the substrate** — concepts are nodes, mappings + hierarchy are edges |
| **Sequence** | ordered list | versions, timelines, audit trail |

**Crosswalker is fundamentally a graph** (concepts + typed edges). Tables, trees, and matrices are *projections of that graph* chosen to answer a question. That single idea — *ontology = graph; every view = a projection of the graph* — is arguably the spine of the whole project and deserves its own concept page.

## The trivialized taxonomy already exists — borrow it

The relief: this is **well-trodden ground in data science**, already compressed into small, learnable "grammars." We don't invent data morphology — we adopt these and point them at ontology/graph data. This is what should inform how the tool names everything it does at the query / output / projection piece.

| Layer | Established grammar | The trivialized verb set | Crosswalker mapping |
|---|---|---|---|
| **Shape** | **Tidy data** (Wickham 2014) | rows = observations, cols = variables; long vs wide | data shapes (relation / tree / graph / sequence) |
| **Transform** | **Grammar of data manipulation** (dplyr; relational algebra, Codd 1970) | one-table: `filter` · `select` · `mutate` · `arrange` · `summarise`+`group_by`; two-table: `join` (inner/left/right/full/anti/semi) · set-ops (∪ ∩ −) | reshape + combine + summarize families |
| **Reshape** | **OLAP cube ops** + pivot (`tidyr`) | `transpose` · long↔wide (`pivot_longer`/`pivot_wider`) · roll-up · drill-down · slice · dice | layout change *without* changing content |
| **Project / present** | **Grammar of graphics** (Wilkinson 1999; ggplot2, Wickham 2010) | data → aesthetics (`x`, `y`, `color`, `size`) → geometry (bar/point/tile/line) → facets / coords | view shapes (table / pivot / graph / hierarchy / timeline) |

**The clarifying payoff** — the Excel examples live in *different* layers:
- **`transpose`** (swap rows ↔ cols) is **reshape** — same content, different layout.
- **"x / y axes"** is **project** — mapping a column to a visual position (an *aesthetic*); the grammar-of-graphics move.
- Excel blurs them into "the pivot table"; data morphology keeps them apart. A pivot table is literally *group + aggregate* (transform) → *long-to-wide* (reshape) → *x = rows, y = cols, fill = measure* (project).

So **one concept page is enough**: **Shape · Transform · Reshape · Project**, each a one-paragraph borrow from an existing grammar, then "here's how Crosswalker exposes it." The operation-families list above folds into **Transform**; reshape + project are the two layers it was missing.

## Candidate views for the GRC corpus (the practical payoff)

Each view = a transform + a reshape + a projection. This menu is what to pull from as we test the audit/compliance library:

| View | Question it answers | Operation family | View shape | Status |
|---|---|---|---|---|
| **Coverage matrix** | where do framework A & B overlap? | summarize (count) | pivot | ✅ built |
| **Coverage density** | how heavily is each area mapped? | summarize (count, grouped) | table / pivot | ✅ built |
| **Gap / orphan** | which controls have *no* coverage? | combine (**anti-join**) | list / table | next |
| **Overlap** | controls in *both* A and B | combine (set-op ∩) | list | buildable |
| **Only-in-A** | in A but not B | combine (set-op −) | list | buildable |
| **Crosswalk chain** | A → B → C transitive reach | traverse (closure) | graph / list | closure shipped |
| **Bidirectional lookup** | what maps to X (either direction)? | reshape (filter) | table | ✅ built |
| **Confidence-weighted coverage** | strong vs weak coverage | summarize (avg/sum of confidence) | pivot heatmap | buildable |
| **Version diff** | what changed v1 → v2? | compare (diff) | table | primitive shipped |
| **Framework-pair density** | which framework *pairs* are densest? | summarize (group by pair) | table | buildable |

The pattern: most "compliance questions" are one operation family + one shape. Naming the family first tells you which primitive to reach for and which shape fits.

## Concept pages to develop (proposal — not building tonight)

1. **`concepts/data-morphology`** (new) — the home for this named concept: the data-shape layer (relation / tree / graph / sequence, and the "ontology = graph, views = projections" spine), the operation families, and how the five layers compose. The other concept pages become its sub-layers.
2. **Extend `concepts/query-primitives`** — group the 8 operations into the 5 families above (reshape / combine / summarize / traverse / compare) instead of a flat list; add the combine-widens-vs-summarize-collapses framing.
3. **Cross-link** [view-shapes](/crosswalker/concepts/view-shapes/) (presentation) + [hierarchy-primitives](/crosswalker/concepts/hierarchy-primitives/) (layout) under the data-morphology umbrella; graduate the [four dials log](/crosswalker/agent-context/zz-log/2026-06-03-four-dials-join-shape-layout-view/) into the layer-composition section.

<Aside type="caution" title="Concept-page sprawl">
Per the user: we may end up with too many concept pages and have to pare them into sections later. Fine for now — capture first, organize once the shape settles. Candidate future grouping: a `concepts/foundations/` section (data-shapes, operations, view-shapes, layout) vs `concepts/domain/` (the GRC/compliance pages).
</Aside>

## Related

- [Four dials — layout · join · shape · view-settings](/crosswalker/agent-context/zz-log/2026-06-03-four-dials-join-shape-layout-view/) — the seed this grows from
- [Query primitives](/crosswalker/concepts/query-primitives/) (Layer A) · [View shapes](/crosswalker/concepts/view-shapes/) (Layer B) · [Hierarchy primitives](/crosswalker/concepts/hierarchy-primitives/) (layout)
- [Ingestion tooling dev log](/crosswalker/agent-context/zz-log/2026-06-02-ingestion-tooling-dev/) · [System architecture](/crosswalker/concepts/system-architecture/) · [Terminology](/crosswalker/concepts/terminology/)
