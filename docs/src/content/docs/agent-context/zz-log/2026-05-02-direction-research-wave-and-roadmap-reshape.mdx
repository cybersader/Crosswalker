---
title: "Direction — research wave (Challenges 08/09/10), critical gaps, and the roadmap reshape"
description: "The direction log promised on 2026-05-01 §6. Three fresh-agent research deliverables landed for Challenges 08 (git audit-trail tenability), 09 (UUID/CWUUID strategy), and 10 (graph→tabular bridging engine). This log summarizes each, calls out substantive gaps in Challenge 10's tech-stack coverage, spins up three new research challenges (11/12/13) to fill those gaps, refreshes the Phase A/B/C/D plan, and translates yesterday's commitments into concrete roadmap deltas."
tags: [foundation, direction, decisions, data-model, audit-trail, identifiers, query-engine, research-callouts]
date: 2026-05-02
sidebar:
  label: "05-02 · Direction (research wave + roadmap reshape)"
  order: -20260502
---

:::note[How to read this log — work in progress]
This is the **direction log** signaled in the [05-01 decision log §6](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#where-this-is-going-next). It does what yesterday's log deliberately did *not* do: translate Foundation commitments into concrete roadmap edits, and spin up follow-on research challenges where gaps remain.

This log is being built up incrementally as decisions firm up. §1–§9 are populated; the proposed roadmap edits in §7 and the StewardshipProfile rename ripples in §8 are **listed for review only — not yet applied to the roadmap or the affected files**. After review, those edits land in their own dedicated commits separate from this log.
:::

## Why this log exists

Yesterday's pair of logs ([orientation AM](/crosswalker/agent-context/zz-log/2026-05-01-foundation-state-of-play/), [decisions PM](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/)) set the table: web-of-webs framing, five Foundation commitments, three new research challenges (08/09/10), the StewardshipProfile rename, the meta-schema lifecycle commitment ("Crosswalker eats its own dog food"), and an explicit deferral of roadmap edits to *this* log.

Today, three things converge:

1. **The 08/09/10 research deliverables landed**, all dropped into the chat by the project owner from fresh-agent research sessions and preserved as `.workspace/2026-05-02-challenge-{08,09,10}-*.md` files (gitignored, repo-local).
2. **A critical reading of those deliverables** surfaced substantive gaps — particularly in Challenge 10's tech-stack coverage. The deliverables make decisions on a shortlist that doesn't exhaust the design space; the gap is non-trivial because the choice of analytical/graph engine touches every downstream architectural question.
3. **The roadmap still reads as it did 2026-04-10**, with Challenge 06 still listed as "biggest open question" (now resolved) and no entries for the StewardshipProfile rename, the meta-schema commitment, the new 08/09/10 challenges, or what comes next.

This log addresses all three: summarizes the research wave (§2), gives a critical reading with explicit gap inventory (§3), spins up new research to fill the gaps (§4), commits the directions that *are* clear (§5), refreshes the phase plan (§6), and lands the actual roadmap deltas (§7). §8 captures the StewardshipProfile rename ripples — the cascading edits flagged yesterday but not yet applied.

## The research wave landed

Three fresh-agent research deliverables, all 2026-05-02:

### 2.1 Challenge 08 — Git history as a compliance audit trail (verdict: augment, not replace)

**Full deliverable**: `.workspace/2026-05-02-challenge-08-git-audit-trail-research.md`. **Brief**: [Challenge 08](/crosswalker/agent-context/zz-challenges/08-git-history-audit-trail-tenability/).

**Headline finding**: bare signed-commit + branch-protection git fails on four of five core audit-evidence standards (SAS 142 "susceptibility to management bias," PCAOB AS 1105 IPE controls, ISO 27001 A.8.15 tamper-resistance against privileged insiders, SOX §802 WORM expectation). It satisfies the **integrity** leg via the Merkle DAG but fails the **trusted-time** and **non-repudiation** legs because the author controls the timestamp and the repository is internally written.

**Recommended Tier 1 hardening** (all required, none deferred to Tier 2/3):

1. Signed commits on every evidence-link state change (SSH signing preferred over GPG)
2. Mandatory remote mirror — refuse to claim "audit trail" for an unmirrored vault
3. **RFC 3161 trusted-timestamp** receipts on every audit-relevant commit (free public TSAs make this operationally negligible)
4. `gc.reflogExpire = never` and `gc.reflogExpireUnreachable = never`
5. Built-in **Audit Authenticity Report** export (FRE 902(13)/(14)-shaped certification PDF/JSON)

**Tier 2** adds nothing audit-wise on its own (the sql.js sidecar is a derived view, not a system of record) but is the right place to *enforce* write-through-to-git in code.

**Tier 3** adds external monitoring (continuous `git verify-commit` + TSA receipt verification + `fsck --full`), per-commit/hourly **WORM mirror** to S3 Object Lock Compliance (or equivalent — Cohasset confirms compliance with SEC 17a-4, FINRA 4511, CFTC 1.31), and SOC 2-attestable infrastructure controls converting the user's IPE problem into the server's IPE problem.

**14-failure-mode inventory** ranked by likelihood and mitigation cost; the residual risk after augmentation is a three-party collusion (admin bypass + WORM compromise + forged TSA), which is below the bar that any commercial GRC tool credibly defends against.

### 2.2 Challenge 09 — UUID / CWUUID cross-cutting identifier strategy

**Full deliverable**: `.workspace/2026-05-02-challenge-09-uuid-cwuuid-research.md`. **Brief**: [Challenge 09](/crosswalker/agent-context/zz-challenges/09-uuid-cwuuid-cross-cutting-strategy/).

**Headline finding**: layered scheme. **UUIDv7** (RFC 9562, May 2024) for almost everything Crosswalker generates; **content-addressed sha256 CIDs** only where the entity *is* its content (spine snapshots, schema releases); **CURIEs** for external references (controls, frameworks, ORCIDs); **ORCID CURIEs verbatim** for SSSOM author/reviewer slots. **"CWUUID" is a display convention, not a new algebra** — every CW-minted ID is a canonical UUIDv7 stored in YAML frontmatter; `cw:` prefix and short hex suffixes are UI affordances only.

**Filename strategy**: human-readable filenames for browseable classes (ontology nodes, evidence notes); composite filenames with `--cw<6-hex>` suffix for collision-prone classes (junction notes, lifecycle records, crosswalk edges with multiple predicates). Aligns with the prevailing Obsidian convention of `uid` in frontmatter (Advanced URI plugin, etc.).

**OSCAL round-trip**: preserve incoming `@uuid` flags verbatim on import; mint UUIDv7 only for entities Crosswalker creates de novo. UUIDv7 is syntactically valid in every OSCAL `uuid` slot — published OSCAL schemas constrain only the regex grammar, not the version nibble. Risk of NIST tightening to v4-only is low.

**Minimum viable Foundation set (v0.1)** — six identifier classes:
1. Vault UUID
2. Ontology-web UUID
3. Ontology-node UUID (alongside CURIE natural key)
4. Junction-note UUID (covers crosswalk + evidence-link)
5. Spine snapshot CID
6. SSSOM author CURIE (ORCID-preferred)

The deliverable is **substantively complete**. Minor gaps flagged in §3.2 below don't warrant a re-run.

### 2.3 Challenge 10 — Graph→tabular bridging engine for the web-of-webs

**Full deliverable**: `.workspace/2026-05-02-challenge-10-graph-tabular-research.md` (truncated copy; full content in chat history). **Brief**: [Challenge 10](/crosswalker/agent-context/zz-challenges/10-graph-to-tabular-bridging-engine/).

**Headline finding**: hybrid 3-tier strategy.

| Tier | Strategy | Engine choice |
|---|---|---|
| **Tier 1** | Build | Materialized-folder generator inside the plugin; flattens graph queries into Bases-compatible YAML notes; per-folder `.view.yaml`, `.view.lock.json` dependency manifest, `.view.stale` flag |
| **Tier 2** | Integrate | **DuckDB-WASM** (~3.2 MB compressed shell, MIT, MotherDuck-backed). Recursive CTEs handle multi-hop traversal; PIVOT/UNPIVOT/window functions handle cross-tabs; Apache Arrow zero-copy to a Polars-JS or Arquero renderer-side pivot layer |
| **Tier 3** | Integrate | **PostgreSQL + Apache AGE** (openCypher graph traversal + full SQL, Apache 2.0, ASF governance). **Oxigraph as RDF sidecar** for SSSOM/SKOS/STRM workloads needing SPARQL property-path closure |

**KuzuDB explicitly rejected** despite Cypher elegance and native property-graph fit, on the basis that the upstream project was archived 10 October 2025 with no upstream maintenance commitment. The deliverable claims this is a load-bearing supply-chain risk for a multi-year compliance tool. ⚠️ Verify before acting on it (see §3.1).

**Five data-flow invariants** — file canonicity, determinism/idempotency, explicit staleness, writes-always-land-in-files, transparent cross-tier query routing. Same posture as the [05-01 §2.5 meta-schema commitment](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#25-crosswalker-eats-its-own-dog-food--every-crosswalker-internal-schema-is-versioned-and-migration-aware): files are the only writable surface; everything else is a content-addressed cache.

**Cost ceilings** sketched per tier (e.g., Tier 1 caps at ~10K vault notes / 30K crosswalk edges; Tier 2 at ~250K notes / 5M edges; Tier 3 effectively unbounded). Marked design-time targets, not benchmarks.

The deliverable is **technically thorough on the shortlist it considered**, but the shortlist is incomplete in ways that matter — see §3.1.

## Critical assessment of the research wave

### 3.1 Challenge 10 has substantive gaps

The deliverable evaluates ~9 engines and makes the DuckDB+AGE call on the basis of that shortlist. Whole classes of relevant systems are not engaged with at all:

| Class | Specific systems missed | Why this matters for Crosswalker |
|---|---|---|
| **Datalog engines** | Soufflé, [Nemo](https://github.com/knowsys/nemo), Differential Datalog, Datomic, RDFox | Datalog is the *native* fit for SSSOM chain rules. Recursive Datalog with provenance is mathematically cleaner than recursive CTE for transitive crosswalk derivation. The OxO2 paper Crosswalker already cites uses Nemo (Datalog). Direct fork in the road that the deliverable doesn't even pose |
| **Production triple stores** | Apache Jena Fuseki, GraphDB (Ontotext), Virtuoso, RDF4J, Stardog, AnzoGraph, Blazegraph | The project is RDF-flavored (SSSOM, SKOS, STRM) by design. Oxigraph is "the WASM one we found"; no comparative analysis against the mature RDF stack |
| **Versioned graph databases** | **TerminusDB** (Git-style branching/diff/merge over RDF) | Terminus's versioned-graph-with-diff-and-merge model is *uncannily aligned* with Crosswalker's "files canonical, derived stores rebuildable" ethos. Should arguably be a top contender; not mentioned at all |
| **Other property graphs** | Memgraph, NebulaGraph, ArangoDB, Dgraph, FalkorDB, OrientDB | The "Kuzu rejected → AGE accepted" leap skips half the relevant landscape |
| **Embedded analytical** | Polars-WASM (Tier 1.5 candidate), DataFusion (Apache), LanceDB, ClickHouse-local, Velox | Polars-WASM as a Tier 1.5 join/pivot layer *without SQL* is a real alternative the deliverable mentions as a renderer-side helper but doesn't seriously evaluate as a primary engine |
| **Vector + graph hybrids** | Weaviate, Qdrant, Milvus, FalkorDB+vec | Becomes critical for AI-assisted schema matching (deferred future workstream). No architectural slot reserved |
| **Streaming / incremental MV** | Materialize, Differential Dataflow, Snowflake Dynamic Tables, ksqlDB | Deliverable cites Postgres MV/BQ/Redshift but skips state-of-the-art incremental view maintenance — directly relevant for the materialized-folder Tier 1 design |
| **Virtual / federated** | Ontop (SPARQL-over-relational), Trino, Dremio | Could turn "files → derived store" into "files → virtual SQL view" without materializing |
| **Query unification** | GraphQL gateway, Substrait | A unified query layer that abstracts the engine choice across tiers. Not mentioned at all |

**Architectural questions left unasked:**

1. Datalog vs recursive CTE for the *core* SSSOM chain-rule derivation. The deliverable picks the harder, less-expressive option without justifying it
2. TerminusDB's versioned-graph model deserves first-class evaluation given Crosswalker's ethos
3. Polars-WASM as Tier 1.5 (without DuckDB) — bundle-size argument deserves deeper treatment
4. GraphQL as a tier-agnostic query surface (compiles to SQL/Cypher/SPARQL per tier)
5. CRDT layer for the deferred live-edit team mode (Yjs / Automerge / Loro)
6. Concrete WASM bundle optimization strategies (tree-shaking, code-splitting, on-demand loading)
7. LLM/NL-query architecture for AI-assisted features
8. **No real benchmarks against representative GRC data** — explicitly out-of-scope per the brief, but means the choices are theoretical against an unmeasured workload

**Empirical claims worth verifying before acting**:

- "**KuzuDB archived 10 October 2025**" — load-bearing for the entire engine choice. Verify upstream + the "bighorn" community fork status
- "**DuckDB-WASM ~3.2 MB compressed**" — confirm against current build; bundle has grown over releases
- "**DuckPGQ extension not yet WASM-friendly**" — check current state
- Apache AGE PostgreSQL version compatibility window

### 3.2 Challenge 09 minor gaps (no new research session needed)

Challenge 09 is substantively complete. Minor flags for implementation only:

- **SHA-256 vs SHA-3 for content addressing**: sha256 fine, but if a downstream user mandates SHA-3 (some federal contexts), allow algorithm agility in the CID prefix
- **UUIDv7 timestamp leakage**: every UUIDv7 reveals creation millisecond — a side-channel in adversarial/forensic settings. Document explicitly
- **UUIDv7-from-mtime migration entropy**: the migration script derives UUIDs from file mtime, which has near-zero entropy. Should be flagged that migrated UUIDs are predictable and unsuitable as security tokens
- **Cross-vault federation protocol**: punted to Phase 2 in the deliverable; the URN form `urn:crosswalker:<vault-uuid>:<entity-uuid>` is sketched but not analyzed against DID:web for vault identity or IPFS CIDs for content

**Action**: lessons-learned annotation on the existing Challenge 09 brief; no new research session.

### 3.3 Challenge 08 — two real omissions

Challenge 08 is broadly complete but skips a category of *modern* attestation primitives that meaningfully change the design:

- **Sigstore + in-toto attestations + SLSA framework** — federated OIDC-backed signing (Sigstore alone could replace the entire "manage GPG/SSH signing keys" UX); in-toto is the standard for "this evidence was reviewed by X using process Y" attestations; SLSA frames the whole supply-chain integrity story. None of these are engaged with
- **OpenTimestamps** (Bitcoin-anchored, free, decentralized) — gets one passing mention in the failure-mode table but isn't compared against RFC 3161 TSAs as an alternative or complement
- **W3C Verifiable Credentials** for the qualified-person certification — flexible alternative to the proposed PDF Audit Authenticity Report
- **AWS QLDB / Azure Confidential Ledger** rejected without deep analysis

**Action**: spin up a narrower follow-on research challenge focused specifically on these primitives — see [§4.3](#43-challenge-13--modern-attestation-primitives) below.

## New research challenges to spin up

Three new fresh-agent briefs to fill the gaps identified in §3. Stub files to be created in `zz-challenges/` once approved; full briefs follow the existing 06/07/08/09/10 pattern.

### 4.1 Challenge 11 — Tier 2/3 graph + analytical engine deep survey

**One-line**: Re-evaluate the Tier 2 and Tier 3 engine choice from Challenge 10 against the full design space, including Datalog engines, production triple stores, versioned graph databases (TerminusDB especially), other property graphs, embedded analytical engines, vector+graph hybrids, streaming/incremental MV systems, and virtual/federated approaches.

**Why now**: Challenge 10's deliverable is technically correct on its shortlist but the shortlist is incomplete. Locking in DuckDB+AGE before Challenge 11 finishes is premature for a multi-year-horizon project.

**Required scope**:

- ≥15 engines evaluated against the same matrix Challenge 10 used (graph fit, tabular fit, browser/server, bundle size, license, project health)
- Explicit TerminusDB analysis — versioned-graph-with-diff-and-merge alignment with Crosswalker's files-canonical ethos
- Explicit Datalog vs recursive-CTE comparison for SSSOM chain-rule derivation (overlaps with Challenge 12 below; coordinate)
- Verification of the empirical claims in Challenge 10 (KuzuDB archival status, DuckDB-WASM bundle size, DuckPGQ WASM readiness)
- Recommend: keep Challenge 10's DuckDB+AGE call, replace, or layer (e.g., Datalog-on-top-of-AGE)

**Out of scope**: actual benchmarks (separate work item per Challenge 02), specific UI design.

### 4.2 Challenge 12 — Datalog vs SQL for SSSOM chain-rule derivation

**One-line**: Narrow brief: should the *core spine derivation engine* (the thing that composes pairwise mappings through a pivot using SSSOM's 22 chain rules) be Datalog or recursive CTE? Independent of the broader Tier 2 engine choice in Challenge 11.

**Why now**: this is a direct fork in the road. The OxO2 prior art uses Datalog; Challenge 10's deliverable uses recursive CTE. Both work, but they have different expressiveness profiles, different debugging stories, different provenance-tracking properties, and different operational maturity.

**Required scope**:

- Formal comparison: SSSOM chain rules expressed as Datalog rules vs as recursive-CTE patterns. Express RCE2 (the rule OxO2 uses) in both
- Provenance: which yields cleaner `derivation_path` records?
- Performance characteristics on representative chains (depth 2–5, 10K–100K mappings)
- Maintainability: which is easier for a fresh contributor to reason about?
- Recommend: Datalog as the primary derivation engine, recursive CTE as the primary, or hybrid (Datalog produces materialized derived rows, queried via SQL)

**Out of scope**: specific Datalog engine choice (covered in Challenge 11).

### 4.3 Challenge 13 — Modern attestation primitives (Sigstore, in-toto, SLSA, OpenTimestamps, VCs)

**One-line**: Narrower follow-on to Challenge 08. Evaluate the modern attestation stack — Sigstore for federated signing, in-toto for review attestations, SLSA for supply-chain framing, OpenTimestamps as alternative/complement to RFC 3161, W3C Verifiable Credentials for the qualified-person certification — and recommend whether these subsume, complement, or are skipped vs Challenge 08's TSA + WORM + cert-export Tier 1 plan.

**Why now**: Challenge 08's recommendation is implementable today, but if Sigstore-style federated OIDC signing replaces the GPG/SSH key UX, that's a Tier 1 architectural difference, not a polish item. Worth knowing before shipping the audit-trail UX.

**Required scope**:

- Sigstore vs RFC 3161 + GPG/SSH for the per-commit signing-and-timestamping primitive
- in-toto attestations vs simple commit messages for the review/approval chain
- SLSA Level 1–4 mapping: where does Crosswalker land, and what does each level cost?
- OpenTimestamps vs RFC 3161 TSAs: complement or replace?
- W3C VCs vs PDF certification report for the qualified-person artifact
- AWS QLDB / Azure Confidential Ledger / immudb deeper-than-one-paragraph evaluation
- Recommend: replace/augment/skip per primitive

**Out of scope**: implementation details, vendor selection.

### 4.4 TerminusDB — short callout pending Challenge 11

TerminusDB warrants a one-paragraph callout *before* Challenge 11 finishes because its versioned-graph-with-diff-and-merge model is so structurally aligned with Crosswalker's files-canonical ethos that not naming it as a top contender feels like a research blind spot. **Decision deferred to Challenge 11**, but this log explicitly flags it as a candidate that must be evaluated, not just "another graph DB on the list."

## Direction posture (adopt or defer per topic)

This log is a stepping stone to the next research wave, not a final stake-in-the-ground. Most of what Challenges 08 and 10 surfaced has follow-on research (Challenges 11/12/13) that could change the answer; only Challenge 09's identifier strategy is mature enough to lock in today.

| Topic | Status | Gates on |
|---|---|---|
| **Identifier strategy** — UUIDv7 + sha256 CIDs + CURIEs + ORCID for SSSOM authors per [Challenge 09 deliverable](#22-challenge-09--uuid--cwuuid-cross-cutting-identifier-strategy) | **Adopt** | None — research substantively complete |
| **Tier 1 materialized-folder generator** — flatten graph queries into Bases-compatible YAML notes per Challenge 10 §2 | **Defer** (concept sound; implementation details depend on engine choice) | [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/) |
| **Tier 1 audit-trail bar** — TSA + WORM mirror + FRE 902(13) cert export per Challenge 08 | **Defer** | [Challenge 13](/crosswalker/agent-context/zz-challenges/13-modern-attestation-primitives/) |
| **Tier 2 embedded engine** — DuckDB-WASM per Challenge 10 | **Defer** | [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/) |
| **Core spine derivation engine** — Datalog vs recursive CTE | **Defer** | [Challenge 12](/crosswalker/agent-context/zz-challenges/12-datalog-vs-sql-sssom-chain-rules/) |
| **Tier 3 server stack** — PostgreSQL + Apache AGE per Challenge 10 | **Defer** | [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/) |
| **Modern attestation primitives integration** — Sigstore, in-toto, SLSA, OpenTimestamps, VCs | **Defer** | [Challenge 13](/crosswalker/agent-context/zz-challenges/13-modern-attestation-primitives/) |
| **StewardshipProfile rename application** across the KB | **Action item** (today) | None — see [§8](#stewardshipprofile-rename-ripples-proposed-edits) |
| **Crosswalker-internal schema versioning policy** per [05-01 §2.5 dog-food commitment](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#25-crosswalker-eats-its-own-dog-food--every-crosswalker-internal-schema-is-versioned-and-migration-aware) | **Defer** (concrete versioning convention) | Direction synthesis after Phase B |
| **Pairwise + optional pivot crosswalk architecture** per [05-01 §2.1](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#21-pairwise--optional-inheritable-pivot-challenge-06-resolution) | **Stays committed** (Challenge 06 resolution) | None |
| **Junction-note evidence-link architecture** per 04-10 synthesis + [05-01 §2.2](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#22-junction-notes-confirmed--but-feasibility-is-tier-aware) | **Stays committed** (form factor) — Tier 1 *audit-trail hardening on top of it* deferred per row above | None for form factor; Ch 13 for hardening |

The two "stays committed" rows are the architectural commitments from yesterday that don't depend on Ch 11/12/13. Everything new from today's research wave is provisional pending the next wave.

## Phase plan refresh

Yesterday's Phase A/B/C, with this log's "defer most things" posture folded in:

- **Phase A — Today's housekeeping (in flight, this log)**
  - Adopt the [Challenge 09 identifier strategy](#22-challenge-09--uuid--cwuuid-cross-cutting-identifier-strategy)
  - Apply the [StewardshipProfile rename](#stewardshipprofile-rename-ripples-proposed-edits) across the KB
  - Land the [proposed roadmap deltas](#proposed-roadmap-deltas) (after user review)
  - Spin up the new fresh-agent challenge stubs ([Ch 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/), [Ch 12](/crosswalker/agent-context/zz-challenges/12-datalog-vs-sql-sssom-chain-rules/), [Ch 13](/crosswalker/agent-context/zz-challenges/13-modern-attestation-primitives/)) — done in this same commit
- **Phase B — Run the next research wave**
  - Hand Challenges 11, 12, 13 to fresh agents
  - Resolve [StewardshipProfile keep/replace/stack](/crosswalker/agent-context/zz-log/2026-04-03-evolution-pattern-taxonomy-draft/) with formal grounding (Stojanovic / Flouris) — already on the [Foundation roadmap](/crosswalker/reference/roadmap/#foundation--get-the-architecture-right)
  - Continue with the existing active challenges (01–05) as appetite allows
- **Phase C — Synthesize**
  - Another decision/direction log distilling Phase B deliverables
  - Likely commits Tier 2 engine, Tier 3 stack, derivation paradigm (Datalog vs SQL), and Tier 1 audit-trail hardening
- **Phase D — Implementation begins**
  - Tier 1 materialized-folder generator — design from Challenge 10 §2 if it survives Phase C, or revised per Challenge 11
  - Tier 1 audit-trail hardening — per the Phase C audit-trail commitment
  - Tier 2 engine integration — once selected
  - Schemas (StewardshipProfile, junction-note, FrameworkConfig v2, `_crosswalker` v2) — implementation following the [meta-schema versioning policy](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#25-crosswalker-eats-its-own-dog-food--every-crosswalker-internal-schema-is-versioned-and-migration-aware) once that policy is concretized
- **Phase E — Out of scope for this Foundation cycle**
  - Marketplace mechanics
  - Cross-vault federation (Phase 2)
  - LLM/AI-assisted features
  - Multi-user collaboration / CRDT layer

The big change vs yesterday's plan: **Phase D is fully gated on Phase C now**. Yesterday I had Phase D running in parallel with Phase B because the Tier 1 design was "committed." Per the user's [defer-till-13 directive](#direction-posture--adopt-or-defer-per-topic), the Tier 1 audit-trail bar isn't actually committed; only the form-factor (junction notes, materialized folders) is. So Phase D waits for Phase C synthesis before any implementation work starts.

## Proposed roadmap deltas

Concrete edits to land in `docs/src/content/docs/reference/roadmap/index.mdx` — **listed here for review; not yet applied**. Edits are bucketed by section of the roadmap.

### A. Foundation — "Get the architecture right" — existing items to update

**A1. "Pairwise crosswalks vs synthetic spine architecture"** *(currently the biggest open Foundation question)*
- ✅ Mark as **resolved** by [05-01 §2.1 deferred-pivot hybrid commitment](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#21-pairwise--optional-inheritable-pivot-challenge-06-resolution)
- Update item description to summarize the commitment: pairwise primary, optional inheritable pivot (default SCF), SSSOM-on-markdown persistence, derived-mappings-computed-not-stored
- Update internal challenge link from `/zz-challenges/06-synthetic-spine` to `/zz-challenges/archive/06-synthetic-spine` (already done in 05-01 commit; verify still correct)

**A2. "EvolutionPattern vs transformation recipes"**
- Rename to: **"StewardshipProfile (formerly EvolutionPattern) vs transformation recipes"**
- Update body text: every "EvolutionPattern" → "StewardshipProfile" with first-mention "(formerly EvolutionPattern)" parenthetical
- Add link to [05-01 §3.2 rename](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#32-stewardshipprofile--the-evolutionpattern-rename-committed)

**A3. "Evidence-framework edge model (the *other* edge type)"** *(currently marked "Direction committed")*
- Add a sentence: **Tier 1 audit-trail hardening (TSA + WORM + cert-export per Challenge 08) is deferred pending [Challenge 13 — modern attestation primitives](/crosswalker/agent-context/zz-challenges/13-modern-attestation-primitives/).**
- Rest of the existing item (junction notes, 13-field schema, OSCAL by-component, etc.) stands

**A4. "Crosswalk edge semantics commitment (STRM + SSSOM)"**
- Mark as ✅ **committed direction** per [05-01 §2.1](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#21-pairwise--optional-inheritable-pivot-challenge-06-resolution) and the Challenge 06 deliverable
- No body changes; status marker only

**A5. "Progressive tier architecture (pillar)"**
- Add: **Tier 2 engine choice deferred pending [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/); Datalog vs SQL fork for derivation deferred pending [Challenge 12](/crosswalker/agent-context/zz-challenges/12-datalog-vs-sql-sssom-chain-rules/).**
- Rename "Tier 2 (sql.js sidecar)" descriptor to "Tier 2 (embedded analytical engine, choice TBD per Ch 11)"
- Existing 3-tier framing stands

**A6. "Obsidian Bases direction research"**
- No change — still active research; coordinates with Challenge 11 but doesn't get superseded

### B. Foundation — new items to add

**B1. NEW: "Identifier strategy (UUIDv7 + sha256 CIDs + CURIEs)"** ✅ committed
- Per [Challenge 09 deliverable](/crosswalker/agent-context/zz-challenges/09-uuid-cwuuid-cross-cutting-strategy/): UUIDv7 default, sha256 multibase CIDs for content-addressed (spine snapshots, schema releases), CURIEs for external references (controls, frameworks, ORCIDs)
- "CWUUID" is display convention only, not a new algebra
- Filename rule: human-readable + `--cw<6-hex>` suffix on collision-prone classes
- Six-class minimum viable Foundation set (vault, ontology web, ontology node, junction note, spine snapshot CID, SSSOM author CURIE)
- OSCAL round-trip: preserve incoming `@uuid` verbatim; mint UUIDv7 only for new entities
- Links to deliverable in `.workspace/2026-05-02-challenge-09-uuid-cwuuid-research.md`

**B2. NEW: "Tier 2/3 engine deep survey"** — research item
- Links to [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/)
- Required to complete before Tier 2 implementation can start

**B3. NEW: "Datalog vs SQL for SSSOM chain-rule derivation"** — research item
- Links to [Challenge 12](/crosswalker/agent-context/zz-challenges/12-datalog-vs-sql-sssom-chain-rules/)
- Coordinates with Challenge 11 (engine choice picks the *paradigm*; Ch 12 picks the engine)

**B4. NEW: "Modern attestation primitives evaluation (Sigstore, in-toto, SLSA, OpenTimestamps, VCs)"** — research item
- Links to [Challenge 13](/crosswalker/agent-context/zz-challenges/13-modern-attestation-primitives/)
- Required before the Tier 1 audit-trail bar from Challenge 08 can be locked in

**B5. NEW: "Crosswalker-internal schema versioning and migration policy"**
- Per [05-01 §2.5 dog-food commitment](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#25-crosswalker-eats-its-own-dog-food--every-crosswalker-internal-schema-is-versioned-and-migration-aware): every Crosswalker-internal schema (StewardshipProfile, junction-note 13-field schema, FrameworkConfig, `_crosswalker` metadata, pivot snapshot manifest, lifecycle change record, SSSOM crosswalk record) is versioned and migration-aware
- Versioning convention TBD (semver vs content-addressed vs release-tag-aligned)
- Direction log next pass to commit the convention

### C. Decision log (bottom of roadmap) — append entries

The current "Decision log" section at the bottom of the roadmap lists 04-03 through 04-09 entries. Append:

- [Foundation state of play (orientation, 05-01 AM)](/crosswalker/agent-context/zz-log/2026-05-01-foundation-state-of-play/) — web-of-webs framing, six open questions
- [Foundation commitments + follow-on research (decisions, 05-01 PM)](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/) — five commitments, three new challenges, StewardshipProfile rename, meta-schema commitment
- [Direction — research wave + roadmap reshape (this log, 05-02)](/crosswalker/agent-context/zz-log/2026-05-02-direction-research-wave-and-roadmap-reshape/) — the present log

### D. Inventory

Total proposed roadmap edits:
- **6 existing items** modified (A1–A6)
- **5 new items** added (B1–B5)
- **3 decision-log entries** appended (C)

These are deltas to the **active** Foundation section only. The Formats / Crosswalks / Evolution / Community sections of the roadmap are not touched in this pass.

## StewardshipProfile rename ripples (proposed edits)

A grep across `docs/src/content/docs` finds **27 files** containing "EvolutionPattern" (last counted 2026-05-02). The rename strategy preserves history but updates the canonical present:

### E1. Concept pages (canonical present — full update)

These pages define the term. Replace "EvolutionPattern" with **"StewardshipProfile"** on first mention parenthesized as "**StewardshipProfile** (formerly EvolutionPattern)", then "StewardshipProfile" thereafter:

- `concepts/terminology.mdx` (3 mentions) — entry header
- `concepts/ontology-evolution.mdx` (3 mentions)
- `concepts/ontology-lifecycle.mdx` (2 mentions)
- `concepts/institutional-landscape.mdx` (2 mentions)
- `concepts/operational-landscape.mdx` (4 mentions)
- `concepts/what-makes-crosswalker-unique.mdx` (8 mentions — most-affected concept page)

### E2. Reference pages — full update (registry + roadmap)

- `reference/registry/cis.mdx` (1)
- `reference/registry/mitre.mdx` (1)
- `reference/registry/nist.mdx` (1)
- `reference/registry/oscal.mdx` (1)
- `reference/roadmap/index.mdx` (3) — covered by §7 A2 above; same rename pass

### E3. Active research challenges — full update

- `agent-context/zz-challenges/03-competitive-landscape.mdx` (1)
- `agent-context/zz-challenges/04-first-principles-audit.mdx` (1)
- `agent-context/zz-challenges/05-transformation-problem.mdx` (1)

### E4. Historical log entries — top-of-file rename callout, body preserved

These logs were written before the rename. Add a single `:::tip` callout at the top of each linking to [05-01 §3.2](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#32-stewardshipprofile--the-evolutionpattern-rename-committed); leave body alone (preserves history). The most-prominent callout goes on the original EvolutionPattern taxonomy draft.

- `agent-context/zz-log/2026-04-03-evolution-pattern-taxonomy-draft.mdx` (4) — **prominent rename callout**
- `agent-context/zz-log/2026-04-03-deep-research-synthesis.mdx` (1)
- `agent-context/zz-log/2026-04-03-distribution-architecture-research.mdx` (1)
- `agent-context/zz-log/2026-04-03-layered-architecture-vision.mdx` (2)
- `agent-context/zz-log/2026-04-03-vision-alignment-decisions.mdx` (2)
- `agent-context/zz-log/2026-04-04-volatility-and-registry.mdx` (1)
- `agent-context/zz-log/2026-04-08-ontology-evolution-first-principles.mdx` (5)
- `agent-context/zz-log/2026-04-09-primitives-depth-and-pluggable-layers.mdx` (1)
- `agent-context/zz-log/2026-04-09-user-first-ontology-maintenance.mdx` (2)
- `agent-context/zz-log/2026-04-10-foundation-research-synthesis.mdx` (12) — **most-mentioned log; prominent callout**

### E5. Today's logs — already use "StewardshipProfile (formerly EvolutionPattern)" pattern; no edit

- `agent-context/zz-log/2026-05-01-foundation-state-of-play.mdx` (6) — already updated
- `agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research.mdx` (12) — the rename log itself
- `agent-context/zz-log/2026-05-02-direction-research-wave-and-roadmap-reshape.mdx` (3) — this log; references the rename in §3.2 link only

### Rename mechanics

- **Categories E1–E3 (canonical present, 14 files)**: surgical Edits replacing "EvolutionPattern" → "**StewardshipProfile** (formerly EvolutionPattern)" on first mention per file, "StewardshipProfile" thereafter
- **Category E4 (historical, 10 files)**: a single inserted `:::tip[Renamed: EvolutionPattern → StewardshipProfile]` callout at the top of each, ~3 lines. Body unchanged
- Total = ~24 file edits in the rename pass

The rename pass should be its own commit, separate from this log's commit, so the diff is reviewable.

## What's still deferred

After this log lands, the following remain **explicitly deferred** to future Foundation work:

| Deferred topic | Gates on |
|---|---|
| Tier 2 embedded analytical engine choice | [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/) deliverable |
| Tier 3 server stack | [Challenge 11](/crosswalker/agent-context/zz-challenges/11-tier-2-3-engine-deep-survey/) deliverable |
| Core spine derivation paradigm (Datalog vs recursive CTE) | [Challenge 12](/crosswalker/agent-context/zz-challenges/12-datalog-vs-sql-sssom-chain-rules/) deliverable |
| Tier 1 audit-trail hardening (TSA + WORM + cert export, or successor) | [Challenge 13](/crosswalker/agent-context/zz-challenges/13-modern-attestation-primitives/) deliverable |
| Concrete versioning convention for Crosswalker-internal schemas (semver vs content-addressed vs release-tag-aligned) | Direction synthesis after Phase B |
| StewardshipProfile keep / replace / stack (formal grounding via Stojanovic & Flouris) | Already on the Foundation roadmap; runs alongside Phase B |
| Marketplace mechanics (Obsidian plugin distribution, signing, sandboxing) | Post-Foundation (Phase 2) |
| Cross-vault federation protocol | Phase 2 |
| LLM/AI-assisted features (NL query, schema-matching assistance, mapping suggestion) | Post-Foundation |
| Multi-user collaboration / CRDT layer | Post-Foundation |
| Marketplace meta-schema (the schema-of-schema question from [05-01 §2.3](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#23-evidence-link-schema-is-our-shipped-default-architecture-supports-a-marketplace)) | Resolved in spirit by [§2.5 dog-food](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/#25-crosswalker-eats-its-own-dog-food--every-crosswalker-internal-schema-is-versioned-and-migration-aware); concrete mechanics still deferred to direction synthesis after Phase B |

This list is intentionally non-exhaustive. The pattern: when a Foundation question can be answered with the *current* state of the world, it's been answered (in [05-01 commitments](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/) or this log). When it depends on Phase B research, it stays deferred.


## Related

**The 05-01 pair (load-bearing for today's direction):**

- [Foundation state of play (orientation, 05-01 AM)](/crosswalker/agent-context/zz-log/2026-05-01-foundation-state-of-play/) — web-of-webs framing, six open questions
- [Foundation commitments and follow-on research (decisions, 05-01 PM)](/crosswalker/agent-context/zz-log/2026-05-01-foundation-commitments-and-followon-research/) — five commitments, three new challenges, the StewardshipProfile rename, the meta-schema commitment

**The research wave (today's deliverables):**

- `.workspace/2026-05-02-challenge-08-git-audit-trail-research.md` (gitignored)
- `.workspace/2026-05-02-challenge-09-uuid-cwuuid-research.md` (gitignored)
- `.workspace/2026-05-02-challenge-10-graph-tabular-research.md` (gitignored)

**The challenges (briefs):**

- [Challenge 08: Git audit-trail tenability](/crosswalker/agent-context/zz-challenges/08-git-history-audit-trail-tenability/)
- [Challenge 09: UUID/CWUUID strategy](/crosswalker/agent-context/zz-challenges/09-uuid-cwuuid-cross-cutting-strategy/)
- [Challenge 10: Graph→tabular bridging engine](/crosswalker/agent-context/zz-challenges/10-graph-to-tabular-bridging-engine/)
- Challenge 11/12/13 stubs **to be created** following user approval (see [§4](#new-research-challenges-to-spin-up))

**Roadmap (target of this log's deltas):**

- [Roadmap root](/crosswalker/reference/roadmap/)
- [Roadmap: Foundation section](/crosswalker/reference/roadmap/#foundation--get-the-architecture-right)
