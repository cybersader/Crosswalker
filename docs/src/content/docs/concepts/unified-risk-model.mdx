---
title: Unified risk model
description: A shared data model (ontology) that internal-audit, GRC/ISRM, and compliance teams can operate on together — with CRI Profile as the authority-of-record and every other framework crosswalked to it. The "assess once, comply many" backbone that any tool (Crosswalker included) can implement.
tags: [uro, unified-risk-model, ontology, cri-profile, grc, audit, compliance, data-model]
sidebar:
  order: 8
---

import { Aside } from '@astrojs/starlight/components';

<Aside type="tip">
The unifying answer to "which tool should each team use?" is **not a single product — it's a shared data model**. Whatever tools internal-audit, GRC/ISRM, and compliance pick, if they all expose the *same* core entities with real relationships, the teams stay relatable instead of running three disconnected databases (or three spreadsheets pretending not to be). This page describes that model. Crosswalker is one way to implement it; so are the [related tools](/crosswalker/concepts/related-tooling/).
</Aside>

## The problem this solves

In a regulated org, internal audit, GRC/ISRM, compliance, and third-party risk each own slices of the same reality — the same controls, frameworks, assets, vendors, processes, findings, and evidence — but their tools model that reality differently (or not relationally at all). The result is duplicated control libraries, crosswalks maintained in ad-hoc spreadsheets, and a reg change that nobody can trace to the controls it affects.

A **unified risk model** (sometimes "unified operational risk model" / URO) fixes this by defining one set of shared entities + relationships that every team operates on through its own front-end — IA in an audit engine, compliance in a reg-CMS, ISRM in a control spine — over the same underlying objects.

## CRI Profile as the spine

For a financial institution, **CRI Profile is the authority-of-record** for the GRC/InfoSec team. Every other framework crosswalks *to* it ([NIST CSF 2.0](/crosswalker/reference/registry/nist/), 800-53, ISO 27001, FFIEC, NCUA) via a standards-based method ([OLIR](/crosswalker/reference/registry/olir/) or [STRM](/crosswalker/reference/registry/strm/)) so the relationships are auditable and survive framework version changes.

The two **load-bearing joints** are the **control** and the **crosswalk**: a single control hangs off one or more CRI diagnostic statements, and because CRI is crosswalked to the other frameworks, that one control plus its evidence automatically answers them too — **assess once, comply many**.

<Aside type="note">
CRI-at-center is the FI default. The same model works with any top-of-house authority — swap CRI for whichever framework your domain treats as canonical (e.g. a biomedical ontology, a library taxonomy). The structure is general; the spine is a choice.
</Aside>

## The shared entities

Legend — ◎ system-of-record owner · ● active user (creates/updates) · ○ consumer/reference · – minimal

| Shared entity | What it is | IA | Compliance | GRC/ISRM | Tie to the CRI spine |
|---|---|---|---|---|---|
| **Authority / Framework** | CRI Profile + crosswalked CSF 2.0, 800-53, ISO 27001, FFIEC, NCUA | ○ | ● | ◎ | The spine — CRI is top-of-house; all others map to it |
| **Requirement / Diagnostic Statement** | testable line items under each authority | ○ | ● | ◎ | CRI diagnostic statements = canonical cyber requirement set |
| **Control (Reference + Applied)** | reusable control library vs. its real implementations | ● tests | ● relies on | ◎ | one control satisfies many CRI statements + crosswalked reqs |
| **Risk / Risk Scenario** | threat × impact to an asset/process | ● risk-based planning | ○ | ◎ (ERM co-owns) | risks tag the CRI function/category they threaten |
| **Asset** (data/systems/facilities) | what you're protecting | ○ | ○ | ◎ (with IT) | scoping — defines *where* CRI controls apply |
| **Vendor / Third Party** | external entities + their services | ○ | ○ | ● security review | TPRM owns SoR; vendor controls assessed vs. CRI-derived reqs |
| **Business Function / Process** (+ BIA: RTO/RPO) | critical processes + tolerances | ○ | ○ | ● | BCP/business owns SoR; criticality drives control rigor (CRI Govern/Identify) |
| **Assessment** (risk / audit / vendor) | a point-in-time evaluation | ◎ audit | ● compliance assessment | ◎ risk | measures adherence to CRI requirements/tiers |
| **Finding / Issue** (+ remediation) | a gap and its action plan | ◎ audit findings | ● | ● | maps back to the CRI requirement/control that failed |
| **Evidence** | proof a control operates | ● relies on | ● | ◎ collect-once | proves CRI controls; reused across every crosswalked framework |
| **Incident** | a realized event | ○ | ○ | ◎ (with SecOps) | maps to CRI Respond/Recover; feeds risk updates |

## How it operates

- **Internal Audit** doesn't get a separate control universe — its tests point at the shared control objects and write back to the shared finding register.
- **Compliance** keeps its own reg-CMS, but maps each regulatory obligation *down* to the same controls, so a reg change surfaces exactly which controls and evidence are affected.
- **GRC/ISRM** owns the spine (authorities, requirements, controls) and the evidence — collected once, reused everywhere.
- Nobody owns everything; each team owns the entities it's accountable for (◎) and reads the rest (○) — which is what keeps it relatable.

## Where Crosswalker fits

Crosswalker implements the **control + crosswalk** core of this model as plain-text notes: concept notes (controls / requirements / framework items), [junction notes](/crosswalker/concepts/terminology/) (the crosswalk edges with typed [STRM](/crosswalker/reference/registry/strm/) relationships), and queries/views (coverage matrices, gap analyses) over them. It is **not** a full GRC platform — it's the relatable backbone the model needs, owned by you in markdown. The teams' other functions (reg-CMS, audit workpapers, TPRM questionnaires) live in [related tools](/crosswalker/concepts/related-tooling/) that connect at the shared control.

How to evaluate whether *any* tool fits this model (the test to apply to every candidate):

1. Do risks, controls, requirements, assets, vendors, processes, findings, evidence exist as **first-class related objects** — not free-text fields?
2. Can one control map to **two frameworks simultaneously** with evidence reuse?
3. Does it import/export **OSCAL** and/or use **OLIR/STRM** crosswalk semantics?
4. Can a control relate to **many** risks/assets/authorities (many-to-many)?

Tools that force a rigid audit-only or vendor-only schema fight the model; tools with a configurable, relationship-rich core align with it.

## Related

- [Related tooling (GRC, audit, compliance, risk)](/crosswalker/concepts/related-tooling/) — the tools each team uses + where Crosswalker fits
- [What makes Crosswalker unique](/crosswalker/concepts/what-makes-crosswalker-unique/) — the plain-text-ownership differentiator
- [Hierarchy primitives](/crosswalker/concepts/hierarchy-primitives/) · [Query primitives](/crosswalker/concepts/query-primitives/) — how the model is emitted + queried
- [Institutional landscape](/crosswalker/concepts/institutional-landscape/) — who creates/maintains/mandates the authorities the spine points at
- [Terminology](/crosswalker/concepts/terminology/) — concept note, junction note, crosswalk edge, evidence link
- Registry: [CRI](/crosswalker/reference/registry/cri/) · [OLIR](/crosswalker/reference/registry/olir/) · [STRM](/crosswalker/reference/registry/strm/) · [OSCAL](/crosswalker/reference/registry/oscal/) · [SSSOM](/crosswalker/reference/registry/sssom/)
- [Framework corpus + tooling checkpoint](/crosswalker/agent-context/zz-log/2026-05-29-framework-corpus-and-tooling-checkpoint/) — the working doc for building a real corpus toward this model
